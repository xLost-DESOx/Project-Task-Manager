const App = () => {
  return (
    <main className="app-shell">
      <section className="hero-card">
        <p className="eyebrow">Project Task Manager</p>
        <h1>Task management made simple.</h1>
        <p className="hero-copy">
          A clean full-stack todo application with backend persistence, task editing,
          completion tracking, filtering, and tests.
        </p>
      </section>
    </main>
  );
};

export default App;
